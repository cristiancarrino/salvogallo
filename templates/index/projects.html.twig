{% extends 'base.html.twig' %}

{% block main %}
    <div class="container">
        <!-- START THE FEATURETTES -->
        <div class="featurette-container">
            <div class="featurette">
                <div class="featurette-image-wrapper">
                    <img class="featurette-image" src="{{ asset('img/5.jpg') }}" alt="{{ 'caption_1' | trans({}, 'projects') }}">
                </div>
                <p class="text-muted">{{ 'caption_1' | trans({}, 'projects') }}</p>
            </div>

            <div class="featurette">
                <h2 class="featurette-heading" id="art2-informa">{{ 'art2_informs' | trans({}, 'projects') }}</h2>

                {{ 'art2_informs_text' | trans({}, 'projects') | raw }}
            </div>

            <hr class="featurette-divider">

            <div class="featurette">
                <h2 class="featurette-heading" id="eventi">{{ 'events' | trans({}, 'projects') }}</h2>

                {{ 'events_text' | trans({}, 'projects') | raw }}
            </div>

            <hr class="featurette-divider">

            <div class="featurette">
                <h2 class="featurette-heading" id="collaborazioni">{{ 'collaborations' | trans({}, 'projects') }}</h2>

                {{ 'collaborations_text' | trans({}, 'projects') | raw }}
            </div>

            <hr class="featurette-divider">

            <div class="featurette">
                <h2 class="featurette-heading" id="networking">{{ 'networking' | trans({}, 'projects') }}</h2>

                {{ 'networking_text' | trans({}, 'projects') | raw }}
            </div>
        </div>
        <!-- /END THE FEATURETTES -->
    </div>
{% endblock %}
