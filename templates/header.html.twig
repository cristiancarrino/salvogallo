<nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('index') }}" id="navbar-left">
        <img class="navbar-logo" src="{{ asset('img/logo.png') }}" alt="Associazione Articolo 2">
        Associazione Articolo 2
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarCollapse">
        <div class="navbar-nav mx-auto">
            <a class="nav-item nav-link {% if app.request.attributes.get('_route') == 'index' %} active{% endif %}" href="{{ path('index') }}">L'associazione <span class="sr-only">(current)</span></a>
            <a class="nav-item nav-link{% if app.request.attributes.get('_route') == 'what' %} active{% endif %}" href="{{ path('what') }}">Cosa facciamo</a>
            <a class="nav-item nav-link{% if app.request.attributes.get('_route') == 'projects' %} active{% endif %}" href="{{ path('projects') }}">Progetti/Eventi</a>
            <a class="nav-item nav-link{% if app.request.attributes.get('_route') == 'extra' %} active{% endif %}" href="{{ path('extra') }}">Extra italiani</a>
        </div>
        <div class="navbar-nav ml-md-3 justify-content-end" id="navbar-right">
            <a class="nav-item nav-link{% if app.request.attributes.get('_route') == 'contacts' %} active{% endif %}" href="{{ path('contacts') }}">Contatti</a>
            <a class="nav-item nav-link dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <img class="locale-flag" src="{{ asset('img/' ~ app.request.locale ~ '.png') }}" alt="{{ "choose_the_language" | trans }}" title="{{ "choose_the_language" | trans }}">
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                    {# ICONE: https://www.iconfinder.com/iconsets/195-flat-flag-psd-icons #}
                    <a class="dropdown-item" href="{{ path('locale', { _locale : 'it'} ) }}"><img class="locale-flag" src="{{ asset('img/it.png') }}" alt="it_IT" title="it_IT"> <span class="ml-2">Italiano</span></a>
                    <a class="dropdown-item" href="{{ path('locale', { _locale : 'en'} ) }}"><img class="locale-flag"  src="{{ asset('img/en.png') }}" alt="en_EN" title="en_EN"> <span class="ml-2">Inglese</span></a>
                </div>
            </a>
        </div>
    </div>
</nav>

{#<nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-dark text-nowrap">#}
    {#<a class="navbar-brand" href="{{ path('homepage') }}" id="navbar-left">Auron Dance Academy</a>#}

    {#<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">#}
        {#<span class="navbar-toggler-icon"></span>#}
    {#</button>#}

    {#<div class="collapse navbar-collapse" id="navbarCollapse">#}
        {#<div class="navbar-nav mx-auto">#}
            {#<a class="nav-item nav-link {% if app.request.attributes.get('_route') == 'who' %} active{% endif %}" href="{{ path('who') }}">Chi siamo <span class="sr-only">(current)</span></a>#}
            {#<a class="nav-item nav-link{% if app.request.attributes.get('_route') == 'courses' %} active{% endif %}" href="{{ path('courses') }}">Corsi e lezioni</a>#}
            {#<a class="nav-item nav-link{% if app.request.attributes.get('_route') == 'contact' %} active{% endif %}" href="{{ path('contact') }}">Contattaci</a>#}
        {#</div>#}
        {#<div class="navbar-nav ml-md-3 justify-content-end" id="navbar-right">#}
            {#{% if not is_granted('IS_AUTHENTICATED_REMEMBERED') %}<a class="nav-item nav-link" href="{{ path('login') }}">Login</a>{% endif %}#}
            {#{% if not is_granted('IS_AUTHENTICATED_REMEMBERED') %}<a class="nav-item nav-link" href="{{ path('registration') }}">Registrati</a>{% endif %}#}
            {#{% if is_granted('ROLE_ADMIN') %}<a class="nav-item nav-link" href="{{ path('admin') }}">Admin</a>{% endif %}#}
            {#{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}<a class="nav-item nav-link" href="{{ path('logout') }}">Logout</a>{% endif %}#}
            {#{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}<a class="nav-item nav-link text-info" href="{{ path('profile') }}"><strong>Ciao {{ app.user.firstname }}!</strong></a>{% endif %}#}
        {#</div>#}
    {#</div>#}
{#</nav>#}

<script>
    const left = $('#navbar-left');
    const right = $('#navbar-right');
    const max = Math.max(left.width(), right.width());
    left.width(max);
    right.width(max);
</script>
